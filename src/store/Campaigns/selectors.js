export const campaignsList = (state) => state.campaigns.campaigns;
export const campaignsError = (state) => state.campaigns.error;
export const campaignsCount = (state) => state.campaigns.count;
export const capmaignStatus = (state) => state.campaigns.status;
